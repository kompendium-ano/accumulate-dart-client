// DO NOT EDIT. Generated by generate-sdk.sh.

{{- range .Types}}

class {{.Name}} {
  {{- range .Values}}
  /** {{.Name}} {{.Description}}. */
  static const {{.Name}} = {{.Value}};
  {{- end}}

  static String name(int value) {
    switch (value) {
      {{- range .Values}}
      case {{.Name}}:
        return "{{lower .Name}}";
      {{- end}}
      default:
        throw new ArgumentError("Unknown {{.Name}} ${value}");
    }
  }

  static int byName(String name) {
    switch (name) {
      {{- range .Values}}
      {{- $value := .Name}}
      case "{{lower .Name}}":
        return {{$value}};
      {{- range .Aliases}}
      case "{{.}}":
        return {{$value}};
      {{- end}}
      {{- end}}
      default:
        throw new ArgumentError("Unknown {{.Name}} '${name}'");
    }
  }
}
{{- end}}